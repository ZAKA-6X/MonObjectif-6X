{
  "name": "monobjectif-client",
  "version": "1.0.0",
  "private": true,
  "description": "Static client build for MonObjectif-6X",
  "scripts": {
    "build": "node build.js",
    "clean": "rm -rf dist"
  }
}
